<div class="form-group comment-form">
  <%= form_for([@report, @report.comments.build]) do |f| %>
    <div class="form-group comment">
      <p><span class="name"><%= current_coach.name %></span><span class="account_name">@<%= current_coach.account_name %></span>としてコメントする</p>
      <div class=""><%= f.text_area :body, rows: 10, class: "form-control", placeholder: "1000文字以内" %></div>
    </div>
    <div class="form-group submit">
      <%= f.submit "コメント送信", class: "btn btn-primary" %>
      <%= link_to "戻る", :back, class: "btn btn-success" %>
    </div>
  <% end %>
</div>